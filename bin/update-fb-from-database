#!/usr/bin/env coffee

require('dotenv').load()
Q = require 'Q'
Q.longStackSupport = true

# _(global).extend require('../lib/utils')

{updateSomeP, updateAllP} = require('../lib/update_firebase_from_changelog')

updateAllP().then((count) ->
  console.log 'Updated', count, 'entities'
  process.exit()
).done()
